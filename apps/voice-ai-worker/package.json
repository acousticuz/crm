{
  "name": "@acoustic-crm/voice-ai-worker",
  "version": "0.1.0",
  "private": true,
  "description": "AI telephone agent — handles inbound calls when staff is unavailable. AGI TCP server + streaming Google STT + Claude conversation + Google TTS, posting collected leads back through the CRM's existing internal endpoints.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/main.js",
    "dev": "ts-node src/main.ts",
    "lint": "eslint src",
    "test": "TS_NODE_TRANSPILE_ONLY=1 node --require ts-node/register --test src/agi/agi-protocol.test.ts"
  },
  "dependencies": {
    "@acoustic-crm/shared": "workspace:*",
    "@google-cloud/speech": "6.7.1",
    "@google-cloud/text-to-speech": "5.6.0",
    "axios": "1.7.7"
  },
  "devDependencies": {
    "@types/node": "20.16.10",
    "ts-node": "10.9.2",
    "typescript": "5.4.5"
  }
}
